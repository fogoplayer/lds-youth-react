import React from "react";
import { render } from "react-dom";
import TextArea from "../../../TextArea";

const Page = props => (
  <div>
    <TextArea id="notes" />
  </div>
);

export default Page;
